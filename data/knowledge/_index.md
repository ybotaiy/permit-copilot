# Knowledge Base Index

The local knowledge base is currently empty.

Run `/ingest` to populate `data/knowledge/` with normalized Markdown sources.

## Expected Layout

- `data/knowledge/nyc-building-code/`
- `data/knowledge/<doc-type>/`

Each knowledge file should include YAML frontmatter plus `##` headings for individual `§` sections when available.

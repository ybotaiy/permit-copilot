# The skill-native core does not read from `.env`.
# If you later add an API-backed UI or automation, configure `ANTHROPIC_API_KEY`
# in Claude Code or your shell environment instead of committing it here.
